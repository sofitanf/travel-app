import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  title: {
    fontSize: 12,
    color: '#000',
  },
  icon: {
    width: 40,
    height: 40,
  },
});

export default styles;
